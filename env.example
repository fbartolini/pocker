# Docker sources configuration
# 
# RECOMMENDED: Use config/docker-sources.json (easiest to edit)
#   - Copy config/docker-sources.sample.json to config/docker-sources.json
#   - Edit the JSON file with your remote Docker sources
#   - The local socket (/var/run/docker.sock) is automatically included unless disabled
#
# Default behavior:
#   - Automatically includes local socket at /var/run/docker.sock (if it exists)
#   - Loads additional sources from config/docker-sources.json if it exists
#
# Environment variables (optional):
#   DOCKER_SOCKET - Override default socket path (default: /var/run/docker.sock)
#   DOCKER_SOCKET_LABEL - Label for local socket (default: "Local")
#   DOCKER_SOCKET_DISABLE - Set to "true" to disable automatic local socket
#   DOCKER_SOURCES_FILE - Override config file path (default: ./config/docker-sources.json)
#
# Legacy: DOCKER_SOURCES env var (only used if no config file exists)
#   DOCKER_SOURCES=[{"name":"edge","endpoint":"http://edge-proxy:2375","label":"Edge"}]
#   DOCKER_SOURCES=edge|endpoint=http://edge-proxy:2375|label=Edge

# Toggle Docker Hub scraping for icons/descriptions.
ENABLE_DOCKER_HUB_SCRAPE=true

# Timeout (ms) for Docker Hub API requests (default: 5000ms = 5 seconds).
# If Docker Hub is unreachable, requests will fail after this timeout instead of hanging.
DOCKER_HUB_TIMEOUT_MS=5000

# Timeout (ms) for Docker API requests (listContainers, info, stats, etc.) (default: 10000ms = 10 seconds).
# If a Docker source is unreachable, requests will fail after this timeout instead of hanging.
DOCKER_API_TIMEOUT_MS=10000

# Cache durations (ms) for description/icon metadata.
DESCRIPTION_CACHE_MS=86400000
ICON_CACHE_MS=604800000

# Optional JSON map of image -> icon URL pairs.
ICON_MAP_FILE=./config/icon-map.json

# Optional max width (e.g. 1400px or 90rem) for the dashboard content area.
# Leave empty to let the layout stretch full width.
PUBLIC_MAX_WIDTH=

# Enable verbose logging for icon/description discovery.
METADATA_DEBUG=false

# Show compose project/stack tags in the UI (default: false).
# Set to "true" to display compose project names as tags on each app card.
SHOW_COMPOSE_TAGS=false

